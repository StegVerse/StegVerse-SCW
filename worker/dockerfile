FROM python:3.11-slim
WORKDIR /app
# context is the repo root; copy from worker/
COPY worker/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY worker/ ./
CMD ["python","worker.py"]
