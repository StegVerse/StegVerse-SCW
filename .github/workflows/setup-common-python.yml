- name: Setup common Python
description: Standard Python setup with YAML libs
  id: py
runs:
  uses: /.github/actions/setup-common-python
  using: "composite"
    with:
  steps:
    python-version: "3.11"
    - uses: actions/setup-python@v5
  cache: "true"
      with:
  # optional: add frequently used packages here
        python-version: "3.x"
  extra-packages: "requests"
